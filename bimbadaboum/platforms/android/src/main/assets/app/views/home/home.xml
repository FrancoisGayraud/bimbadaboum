<Page loaded="loaded" actionBarHidden="true">
    <WrapLayout padding="10vh">
			<Button text="Profil" class="profileButton" tap="editProfil"/>
    	<label text="{{ 'Bonjour ' + name + ' !'}}" class="mainTitle"></label>
			<StackLayout backgroundColor="#f4dcdc" class="matchZone">
				<label text="Recherche" class="searchTitle"></label>
   			<Button text="Match !" class="matchButton" tap="searchMatch" verticalAlignment="center"/>
			</StackLayout>
    	<Button text="DECONNECTION" class="disconnectButton" tap="logout"/>
    </WrapLayout>
</Page>
